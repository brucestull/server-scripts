# .secrets.example
#
# Copy to: scripts/.secrets
# Then edit values as needed.
#
# IMPORTANT:
# - Do NOT commit scripts/.secrets (real secrets)
# - Keep this file committed as the template

# SSH / remote connection defaults
USER_NAME="your_remote_username"
SSH_KEY_PATH="/absolute/path/to/your/private_key"

# Optional (only used by shutdown script fallback)
# Leave empty unless a script explicitly prompts/uses it.
PASSWORD=""

# Optional (if you want a default domain appended to bare hostnames)
# Example: ".local" -> "SPINAL-TAP" becomes "SPINAL-TAP.local"
# HOSTDOMAIN=".local"

# Options for creating a new user (when using user bootstrap/add-user scripts)
NEW_USER_NAME="new_username_to_create"
NEW_USER_PASSWORD="REPLACE_WITH_A_STRONG_PASSWORD"
NEW_USER_PUBKEY_PATH="/absolute/path/to/new_user_public_key.pub"
